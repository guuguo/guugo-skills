# 两个 GG 技能落地验收（2026-09-08）

## 范围与结果

新增 gg-drama-library 与 gg-video-analysis；技能规范、脚本、本机配置、安装入口和客户端链接完成。正式素材库已初始化，作品/桥段数量为 0。没有执行批量采集或完整作品叙事验收。

维护源：guuguo-skills/skills 下的两个同名目录。规范入口：~/.agents/skills/<name> 相对软链接。仓库已有未提交改动原样保留，未提交、推送或改动原字幕技能。

## 可重复校验

- 两个技能 quick_validate：通过。
- node --check scripts/install.js：通过。
- python3 skills/gg-drama-library/scripts/test_library.py：7 项通过。覆盖跨集记录、切换 cwd 后配置生效、时间越界、错误媒体版本、悬空回合引用、无匹配/局部结果过滤、索引后修订可查和切库保留旧库。
- 4 秒 10fps 合成视频，以 0.2 秒间隔取 20 帧，实际时码为 0.0–3.8；修复浮点比较漏采后验证通过。
- 请求窗口 1.05–1.75 秒，实际取帧为 1.1/1.3/1.5/1.7 秒，未将请求时刻冒充帧时刻。
- 已查看合成样本全部 2 页 4×4 接触表，末页仅 4 帧，其余留空，无重复充数；另查看真实对白样本全部 1 页。
- 现有《回乡》第 4 集前 8 秒临时副本：prepare + 本地 MLX Whisper ASR 成功，2 个 segment，包含词级时间戳。音频与抽帧、ASR 重阶段串行；预检 caution（系统负载），未发生 block，运行时记录本进程 RSS/内存。
- 原证据目录非空时拒绝覆盖；无效采样范围拒绝；已有 ASR 原始结果拒绝覆盖：通过。

## 配置

- ~/.config/gg-drama-library/config.json：library_root = /Volumes/Seamless SSD/素材库/短剧桥段素材库
- ~/.config/gg-video-analysis/config.json：独立分析默认输出到上述库的 analysis，包含本机 Python、本地模型 snapshot、资源预检脚本位置；素材库调用时由 --output 指定单集证据目录。
- 配置与原媒体不进入技能仓库。资源预检复用已安装 video-subtitle-workflow，不复制字幕渲染/OCR 管线。

## 客户端状态

| 客户端 | gg-drama-library | gg-video-analysis |
| --- | --- | --- |
| Codex | linked | linked |
| Claude | linked | linked |
| OpenCode | linked | linked |
| Antigravity | linked | linked |
| Qoderwork | linked | linked |
| Hermes | linked（imported） | linked（imported） |

Antigravity 的 chat.useClaudeSkills 已确认为 true。

## 实际限制

本次验证了工具和数据链路，没有以完整作品验证叙事解释、人物适配排序或采集来源适配。真实对白样本未直接听审；ASR 人名与缩小字幕存在疑点，保留原结果，不宣称转写准确。素材库首版为词项召回加 Agent 语义比较，无向量服务或常驻调度。

结构校验不替代观看证据；细粒度帧/ASR 锚点与推断由 Agent 回查。外置卷离线停止写入的门禁已实现，本次未卸载用户磁盘做破坏性演练。

## 后续变更：自动触发与受众分类

根据用户明确要求，两个技能显式设置 allow_implicit_invocation=true；视频分析入口增加自然语言触发场景，完整内容分析默认输出 content_profile 和 audience。规范区分核心/次级受众、兴趣、情绪需求、年龄/人生阶段、观看门槛与定位，并要求证据与未知说明。真实平台画像另存 audience_metrics，不与内容推断混淆。

单集/作品/桥段数据约定与检索流程已承接受众分类，索引加入可选 audience 字段，旧记录保持兼容。普通视频分析不自动入短剧库，窄转写任务不强制扩大到全片受众分析。

验证：8 项库测试通过（新增受众召回与索引保留测试）；两个技能结构校验通过；自动发现配置为 true，规范引用和 Codex/规范入口均指向维护源。此次未重新运行媒体预处理，也未用真实作品评估分类准确度；自动选择最终由宿主 Agent 根据需求匹配执行，未进行独立会话触发实验。

## 后续变更：直接找相似爆款，采集视频数据

按用户要求取消默认先选 AI 方向的步骤，改为直接检索并展示真实相似参考、完整桥段和数据证据。新增 performance-data 规范，覆盖来源与发布信息、播放互动、可得后台指标、近似显示、未知原因、历史快照及可比爆款依据。

record-metrics 命令新增真实快照落盘接口（不含平台爬虫），检查时间、数值、集数与证据路径，同 ID 拒绝覆盖；检索与索引携带对应发布条目的最新快照，搜索文本排除数据证据以避免无关召回。旧素材没有快照仍可读，显示数据不足。

11 项测试通过，新增历史快照保留/最新关联/覆盖拒绝、未知不等于零/缺集拒绝、指标文字不影响内容检索三项；技能结构与文档引用通过。本次没有实际采集平台数据或认证任何爆款。

## 用户修正目标：素材支持的创意发散，人来定稿

已将 gg-drama-library 默认目标调整为先看真实精品/爆款参考，再按创意需求给出有不同互动与后果的灵感种子；无需用户先选抽象方向或某个案例。每条区分参考来源、新构思和假设，纯 AI 构思无出处时明确标注。只查原例时不强加创意，表达兴趣不等于采纳或定稿。

更新入口、检索参考、查询 ideas 数据约定、UI 文案和验收标准；保留原作证据、受众与表现数据链路。历史评审中的自动成稿要求已标明不属于当前目标，三个已复现的工具问题仍未修复。本次仅修改规范，未重跑媒体或脚本测试；技能结构、引用和 YAML 检查通过。不将规范更新当成创意质量或真实用户效果已验证。

## 用户调整检索职责：Harness 自主搜索

已删除 library.py 的 search、字符串命中评分和默认候选截断。工具保留配置、初始化、指标追加、结构校验与全量目录导出；目录增加触发和互动回合并保留所有状态。搜索路径、语义相似性、精品/表现比较和证据核对由 Harness 按 references/retrieval.md 指引执行，不把无词命中当作无案例。

同步更新数据约定、验收标准和历史评审状态；旧搜索测试替换为全量 16 条目录/局部状态保留/动作内容可读、目录重建、受众与最新指标保留检查。工具测试只证明目录与存储行为，不证明 Harness 搜索效果；未执行真实作品语义检索实验。证据文件缺失仍可标 usable 的既有门禁问题未在本轮修复。
